<UserControl x:Class="ICare.Views.AppsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource PanelSurfaceBrush}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:design="clr-namespace:ICare.Design"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <TextBox
            Name="SearchBox"
            Grid.Row="0"
            TextChanged="SearchBoxTextChanged"/>
        
        <ListBox
            Name="ListBox"
            Grid.Row="1"
            ItemsSource="{Binding Apps}"
            d:DataContext="{d:DesignInstance Type=design:DesignApps, IsDesignTimeCreatable=True}"
            SelectionChanged="ListBox_OnSelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding Icon}" Width="24" Height="24" Stretch="Uniform"/>
                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Margin="5,0,0,0"
                                   FontSize="16"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>