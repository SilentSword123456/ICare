<Window x:Class="ICare.Views.BreakOverlayWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ICare.Views"
             mc:Ignorable="d"
             SizeToContent="WidthAndHeight"
             WindowStyle="None"
             AllowsTransparency="True"
             Background="Transparent"
             Topmost="True"
             ShowInTaskbar="False"
             WindowStartupLocation="Manual">
    
    <Border Background="#20FFFDD0" CornerRadius="40" Padding="10">
        <Grid>
            <TextBlock Foreground="AntiqueWhite" 
                        FontSize="36"
                        VerticalAlignment="Top" 
                        HorizontalAlignment="Center">
                <Run x:Name="BreakOverlayMessage" Text=" We believe you are in a meeting."/>
                <LineBreak/>
                <Run Text="Press "/>
                <Run x:Name="SkipKeyRun" Text="Control+Shift+Q" FontFamily="Consolas" FontWeight="Heavy" Background="DarkSlateGray"/>
                <Run Text=" if you wish to skip the break"/>
            </TextBlock>
            <TextBlock x:Name="CountdownText"
                       Text="5"
                       Foreground="White"
                       FontSize="72"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Top"
                       Margin="0,100,0,0" />
        </Grid>
    </Border>
</Window>
