<UserControl x:Class="ICare.Views.ConfirmResetDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:"
             MinWidth="320"
             MaxWidth="380">
    
    <StackPanel Margin="24">
        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
            <materialDesign:PackIcon Kind="AlertCircleOutline"
                                     Width="22" Height="22"
                                     Foreground="#D32F2F"
                                     VerticalAlignment="Center" />
            <TextBlock Text="Restore app defaults?"
                       FontSize="16" FontWeight="Medium"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center" />
        </StackPanel>

        <TextBlock Text="This will reset all settings to their default values. This cannot be undone."
                   TextWrapping="Wrap"
                   Foreground="#6B6B6B"
                   FontSize="13"
                   Margin="0,0,0,20" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    IsCancel="True"
                    Margin="0,0,8,0"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    CommandParameter="{x:Static local:ResetDialogResult.Cancel}" />
            <Button Content="Reset"
                    Style="{StaticResource DangerFilledButton}"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    CommandParameter="{x:Static local:ResetDialogResult.Confirm}" />
        </StackPanel>
    </StackPanel>
</UserControl>